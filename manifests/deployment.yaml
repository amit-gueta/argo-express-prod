apiVersion: v1
kind: Service
metadata:
  name: express-server
  namespace: prod
  labels:
    app: express-server
spec:
  ports:
  - port: 3000
    protocol: TCP
  clusterIP: None
  selector:
    app: express-server
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: express-server
  namespace: prod
  labels:
    app: express-server
spec:
  replicas: 1
  selector:
    matchLabels:
      app: express-server
  template:
    metadata:
      labels:
        app: express-server
    spec:
      containers:
      - name: express-server
        image: guetaamit/app:5ed29135c0c6d2568d05dc682cb9aaaf36aa02b1
